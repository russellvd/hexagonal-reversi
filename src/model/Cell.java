package model;

/**
 * An Enumeration of the possible contents of a cell where WHITE represents a white tile in the
 * cell, BLACK represents a black tile in the cell, and EMPTY represents a cell with no tile.
 */
public enum Cell {
  WHITE, BLACK, EMPTY;
}
